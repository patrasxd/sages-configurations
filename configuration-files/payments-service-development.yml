server:
  port: 10000 #${random.int(10000,10010)}
spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:file:./data/payments-development.db;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
